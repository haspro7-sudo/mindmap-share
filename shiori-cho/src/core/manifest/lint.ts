// STUB (contract) — creator-side lint (warnings + errors) on a StudioProject.
import type { StudioProject, ValidationIssue } from '../types';

export declare function lintProject(project: StudioProject): ValidationIssue[];
