// STUB (contract) — IndexedDB StudioRepo (DB 'shiori-studio', version 1, store 'projects' keyPath 'id').
import type { StudioRepo } from './repo';

export declare function openIdbStudioRepo(dbName?: string): Promise<StudioRepo>;
