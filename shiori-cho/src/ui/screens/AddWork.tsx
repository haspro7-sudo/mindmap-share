// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/add (F5 import file/drop/paste → ImportPreview, F6 かんたんしおり, 記録だけ, サンプルを試す).
import type { ReactNode } from 'react';
export function AddWorkScreen(): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
