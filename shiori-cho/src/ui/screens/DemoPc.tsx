// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/demo-pc PC画面シミュレータ (F17 AC3).
import type { ReactNode } from 'react';
export function DemoPcScreen(): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
