// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/help (F19).
import type { ReactNode } from 'react';
import type { HelpSection } from '../../core/route';
export function HelpScreen(_props: { section?: HelpSection }): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
