// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/code?w=<id> (F10).
import type { ReactNode } from 'react';
export function CodeEntryScreen(_props: { workId?: string }): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
