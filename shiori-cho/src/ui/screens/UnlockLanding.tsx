// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/u/<manifestWorkId>/<code> (F11): handleDeepLink then replaceState to #/w/<localId> or #/code.
import type { ReactNode } from 'react';
export function UnlockLanding(_props: { manifestWorkId: string; code: string }): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
