// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/w/<id>/edit 作品設定.
import type { ReactNode } from 'react';
export function WorkEditScreen(_props: { workId: string }): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
