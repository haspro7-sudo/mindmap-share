// STUB (UI contract) — F2 AC3: plain 「メモ」 notepad (settings.camouflageText); 1 s long-press on the heading returns (or PIN when set).
import type { ReactNode } from 'react';
export declare function Camouflage(props: { onReturn(): void }): ReactNode;
