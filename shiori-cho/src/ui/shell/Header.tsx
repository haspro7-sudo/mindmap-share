// STUB (UI contract) — global header: back button (non-tab routes), 「しおり帳」, 「隠す」 button.
import type { ReactNode } from 'react';
export declare function Header(props: { onHide(): void }): ReactNode;
