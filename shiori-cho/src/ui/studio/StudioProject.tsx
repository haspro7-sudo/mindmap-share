// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/studio/<pid>?tab=… 工房 editor (F16).
import type { ReactNode } from 'react';
import type { StudioTab } from '../../core/route';
export function StudioProjectScreen(_props: { projectId: string; tab: StudioTab }): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
