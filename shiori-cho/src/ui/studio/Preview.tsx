// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/studio/<pid>/preview: WorkScreen on a MemoryRepo seeded from the build (F16 AC5).
import type { ReactNode } from 'react';
export function StudioPreviewScreen(_props: { projectId: string }): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
