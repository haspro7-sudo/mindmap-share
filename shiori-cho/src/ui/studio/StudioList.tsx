// PLACEHOLDER (UI contract) — replace with the real screen; keep the export name/props. — #/studio 工房 list (F16 AC1, AC7).
import type { ReactNode } from 'react';
export function StudioListScreen(): ReactNode {
  return <main className="screen"><p className="muted">準備中</p></main>;
}
